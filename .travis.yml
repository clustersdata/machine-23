language: cpp

sudo: required

os:   linux
dist: trusy

addons:
  apt:
    sources:
    - sourceline: ppa:beineri/opt-qt542-trusty
    packages:
    - qt5base-dev

env:
  global:
  - QTDIR=/opt/qt54 PATH=$PATH:$QTDIR/bin


compiler: 
- clang
- gcc

script:
- mkdir build
- cd build
- cmake ..
- make
- make test
