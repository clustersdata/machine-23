# todo: write appveyor yml config

image: Visual Studio 2015

platform: x64

configuration: Release

environment:
    QTDIR: "C:\\Qt\\5.7\\msvc2015_64"
    PATH: "%PATH%;C:\\Qt\\5.7;C:\\Qt\\5.7\\msvc2015_64\\bin"

#before_build:


build_script:
  - mkdir build
  - cd build
  - cmake -G "Visual Studio 14 2015 Win64" ..
  - cmake --build .

build: off # disable automatic build

test_script:
  - cmake --build . --target test
